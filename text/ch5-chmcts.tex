
% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision

% [ ] Latex problems
% [ ] Latex labels























% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\chapter{\label{ch:5-chmcts}Convex Hull Monte Carlo Tree Search}

    \minitoc

    This chapter considers multi-objective Monte Carlo Tree Search 
    (MOMCTS) for the decision support scenario (Figure \ref{fig:2:mo_decision_support}) with linear utilities, where the agent must plan for an unknown weight vector $\bff{w}$ over the objectives. Two approaches are considered in this chapter, the first following the same design paradigm as UCT where an algorithm for CMAB problems (Section \ref{sec:2-1-3-contextual-mab}) is applied at each decision node; the second is to maintain a convex hull value set at each node in the 
    search tree, representing the set of achievable expected returns 
    across all possible weightings over the objectives which is updated using convex hull backups (Section \ref{sec:2-6-1-chvi}). 
    
    This chapter is organised as follows:
    \begin{itemize}
        \item Section \ref{sec:5-1-intro} motivates the 
            multi-objective decision making framework used in this 
            chapter, and generalises the notions of simple and cumulative regret to 
            \textit{contextual simple regret} and 
            \textit{contextual cumulative regret} for the multi-objective setting;
        \item Section \ref{sec:5-2-czt} introduces 
            \textit{Contextual Zooming for Trees} (CZT), a 
            MOMCTS algorithm following the same design paradigm 
            as UCT;
        \item Section \ref{sec:5-3-chmcts} generalises prior work 
            in MOMCTS to stochastic environments through 
            \textit{convex hull backups};
        \item Section \ref{sec:5-5-results} presents empirical 
            results on Deep Sea Treasure environments and a range 
            of multi-objective environments from the 
            Multi-Objective Gymnasium library \cite{mo_gym};
        \item Section \ref{sec:5-6-theory} provides a brief 
            theoretical discussion of the MOMCTS algorithms 
            introduced in this chapter.
    \end{itemize}


















% [X] First Draft
% [X] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\section{Introduction and Motivation}
\label{sec:5-1-intro}

    This section motivates the use of \textit{contextual tree search} 
    for planning in multi-objective settings, and highlights why 
    consistent action selection with respect to a fixed objective 
    is a necessary component of effective MOMCTS. The notions of 
    cumulative and simple regret are then generalised to the 
    multi-objective setting.

\subsection{Consistent Action Selection}

    \begin{figure}
        \centering
        \includegraphics[width=0.6\textwidth]{figures/ch5/motivate_contextual_momdp.pdf}
        \caption[A motivating example MOMDP for contextual tree search.]{A motivating example MOMDP for contextual tree search. The convex hull value set at the root consists of $\{(4,0),(0,4)\}$, following the red paths. The optimal values at the root node require specific actions at the child nodes (blue), while at the child nodes all actions are \textit{locally optimal}.}
        \label{fig:5:motivating_example}
    \end{figure}

    Consider the MOMDP illustrated in Figure \ref{fig:5:motivating_example}, with two objectives. The convex hull value set of the MOMDP is $\{(4,0),(0,4)\}$: the reward $(4,0)$ is only reachable via the left action, and $(0,4)$ only via the right action. The optimal action at the root node therefore depends entirely on the weight vector $\bff{w}$: if $w_1 > w_2$ then the left action is optimal, and if $w_2 > w_1$ then the right action is optimal.

    Once the agent has taken the left action from the root node, it has implicitly committed to optimising towards the first objective (i.e. $w_1 > w_2$) because the rewards available from the right action are no longer reachable. Although all actions at the immediate child nodes are \textit{locally optimal} (i.e. they lead to a value in the local convex hull value set), only the action leading to $(4,0)$ is consistent with the implicit commitment made at the root node.

    \begin{figure}
        \centering
        \begin{subfigure}{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/locally_optimal.pdf}
            \label{fig:5:motivating_vanmoffaert:sub:a}
        \end{subfigure}
        \begin{subfigure}{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/locally_optimal_interleved.pdf}
            \label{fig:5:motivating_vanmoffaert:sub:b}
        \end{subfigure}
        \caption[Example of locally optimal actions that are not globally optimal.]{Example of locally optimal actions that are not globally optimal (reproduced from \citet{locally_optimal}). (a) The three globally optimal paths in green, red and blue. (b) The black path interleaves actions from the green, red and blue paths where actions are locally optimal at each step, but is a globally suboptimal path.}
        \label{fig:5:motivating_vanmoffaert}
    \end{figure}

    The problem of \textit{consistent action selection} is also discussed by \citet{locally_optimal}. They compute and track Pareto-optimal policies using an adaptation of Q-learning and highlight that even if an action is \textit{locally optimal}, it may not be \textit{globally optimal}. To demonstrate this, they use an example with three optimal paths (Figure \ref{fig:5:motivating_vanmoffaert}), where the black path alternates between multiple optimal paths, taking only locally optimal actions at each step but fails to follow any single globally optimal path.


\subsection{Contextual Tree Search}
\label{sec:5-1-1-contextual_tree_search}

    \citet{locally_optimal} solve the consistent action selection problem by explicitly tracking the optimal policies across the full MOMDP. The approach taken in this thesis is to consider a \textit{contextual tree search}, where a context weight vector $\bff{w}$ is sampled at the start of each trial and used to guide action selection throughout that trial. In the context of MORL with linear utilities, this is sufficient to resolve the consistent action selection problem, ensuring that all actions within a trial are consistent with a single objective.

    In the \thtspp\ewe framework, the context weight $\bff{w}$ is sampled during the \samplecontext\ewe function (Section \ref{sec:2-4-1-thts}) and passed to all subsequent steps of the trial, including the search policy and backup functions.

    \begin{defn}
        \label{def:5:context_policy}
        A \textnormal{contextual policy} $\pi(a|s;\bff{w})$ is a 
        policy that may depend on the context weight vector 
        $\bff{w}\in\Delta^{D}$, where $\pi(a|s;\bff{w})$ gives 
        the probability of selecting action $a$ at state $s$ under 
        weight $\bff{w}$.

        A policy $\pi$ is \textnormal{context independent} if 
        $\pi(a|s;\bff{w}) = \pi(a|s;\bff{w}')$ for all 
        $\bff{w},\bff{w}'\in\Delta^{D}$ and all $s\in\cl{S}$, 
        $a\in\cl{A}$. Otherwise, $\pi$ is 
        \textnormal{context dependent}.
    \end{defn}

    To reason about the performance of a contextual tree search algorithm, the notion simple regret is generalised to \textit{contextual simple regret}.

    % \begin{defn}
    %     \label{def:5:contextual_cumulative_regret}
    %     Let $\{\bff{w}^i\}_{i=1}^n$ be a sequence of weight vectors sampled uniformly from $\Delta^D$. The \textnormal{contextual cumulative regret} of the search policies $\pi^{1:n} = (\pi^1,\ldots,\pi^n)$ after $n$ trials is:
    %     \begin{align}
    %         \ctxreg(s,\pi^{1:n}) = 
    %             \sum_{i=1}^n \left( 
    %                 \max_{\pi'} {\bff{w}^i}^\top \bff{V}^{\pi'}(s)
    %                 - {\bff{w}^i}^\top \bff{V}^{\pi^i}(s)
    %             \right).
    %     \end{align}
    % \end{defn}
    
    \begin{defn}
        \label{def:5:contextual_simple_regret}
        The \textnormal{contextual simple regret} of a recommendation policy $\psi$ at state $s\in\cl{S}$ is:
        \begin{align}
            \ctxsreg(s,\psi) = \bb{E}_{\bff{w}\sim U(\Delta^D)}\left[
                \max_{\pi'} {\bff{w}}^\top \bff{V}^{\pi'}(s) 
                - {\bff{w}}^\top \bff{V}^{\psi(\cdot;\bff{w})}(s)
            \right],
        \end{align}
        where $\bff{w}\sim U(\Delta^D)$ denotes a weight vector sampled uniformly from the unit $(D-1)$-dimensional simplex.
    \end{defn}

    % Several prior MOMCTS works \odot{cite}, discussed in Section \odot{ref ch3}, use a context-independent search policy. It is shown in Section \ref{sec:5-6-theory} that any context-independent search policy suffers linear contextual cumulative regret, and any context-independent recommendation policy suffers linear contextual simple regret.

    % \odot{Theorem that context independent search policy suffers linear contextual cumulative regret AND context independent recommendation policy suffers linear contextual simple regret}



















% [X] First Draft
% [X] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\section{Contextual Zooming for Trees}
\label{sec:5-2-czt}

    A common design paradigm in MCTS is to adapt a MAB algorithm to run at each decision node of the search tree, treating the node as a (non-stationary) bandit problem. UCT follows this paradigm by running UCB at each node (Section \ref{sec:2-4-2-uct}). 
    
    The \textit{Contextual Zooming} (CZ) algorithm \cite{cz,cz_long} is a generalisation of the UCB algorithm to contextual bandits (Section \ref{sec:2-1-3-contextual-mab}). Following the same methodology, \textit{Contextual Zooming for Trees} (CZT) runs the CZ algorithm at each decision node of the search tree. 
    
    CZT is presented in this section in the context of MOMCTS, where the context is the weight vector $\bff{w}$ sampled at the start of each trial. Although CZ, and by extension CZT, is not motivated by the exploration setting this thesis focuses on, it is introduced as a natural contextual analogue of UCT, and as a contextual search policy baseline for MOMCTS, where no such baseline currently exists.

\subsection{Algorithm Definition}
\label{sec:5-2-1-czt}

    Contextual Zooming operates over a similarity space (defined in Section \ref{sec:2-1-3-contextual-mab}), which in the case of CZT is the weight-action space $\Delta^D\times\cl{A}$. The following metric is defined over the weight-action space for use in CZT:
    %
    \begin{align}
        \dczt((\bff{w},a),(\bff{w}',a')) = \| \bff{w} - \bff{w}' \|_2 + C_{\textnormal{czt}}\one\left[a\neq a'\right], \label{eq:5:czt_metric}
    \end{align}
    %
    where $\Cczt>\sqrt{2}$ is a large constant such that the distance between any two actions is always greater than the distance between any two weights.

    CZ maintains a set of \textit{active balls} that cover the weight-action space. 
    In CZT, let $A(s,a)$ denote the set of active balls for action $a$ at decision node $s$, where a ball with centre $(\bff{w},a)$ and radius $r$ is defined as $\ball((\bff{w},a),r) = \{(\bff{w}',a')\in\Delta^D\times\cl{A} \mid d((\bff{w},a),(\bff{w}',a')) < r\}$. Let $A(s)$ be the set of all active balls at decision node $s$, i.e. $A(s) = \cup_{a\in\cl{A}} A(s,a)$.

    Each set of active balls is initialised to a single ball, using the centroid of the simplex $\frac{1}{D}\bff{1}$, where $\bff{1}$ is the vector of all ones. Specifically, 
    \begin{align}
        A^0(s,a) = \left\{ \ball\left(\left(\frac{1}{D}\bff{1},a\right),\sqrt{\frac{D-1}{D}}\right) \right\}. \label{eq:5:czt_initial_ball}
    \end{align}

    Each ball $B\in A(s,a)$ has an associated visit count $M(B)$ and average return value estimate $\Qczt(B)$. The visit count and value estimates are initialised to $M(B)=0$ and $\Qczt(B)=\bff{0}$ respectively.

    Two rules are applied to run contextual zooming: 
    \begin{itemize}
        \item the \textit{selection rule}, to select a ball (and action) from the set of active balls at the current decision node, as part of the \selectaction\ewe function;
        \item the \textit{activation rule}, which optionally adds a ball with smaller radius to the set of active balls, as part of the \backupv\ewe function.
    \end{itemize}
    
\paragraph{Selection Rule}

    Let $\bff{w}$ be the context weight sampled for the $n$th trial, let $r(B)$ denote the radius of a ball $B\in A(s,a)$. The \textit{domain} of $B$ in CZ consists of the portion of the ball not covered by any smaller active ball. Specifically,
    \begin{align}
        \dom(B) = B - \left( \bigcup_{B'\in A(s,a):r(B')<r(B)} 
            B' \right). \label{eq:5:czt_domain}
    \end{align}

    The \textit{confidence radius} $\conf(B)$ of ball $B$ is:
    \begin{align}
        \conf(B) = \bczt \sqrt{\frac{\log(N(s))}{1+M(B)}},
            \label{eq:5:czt_conf_radius}
    \end{align}
    where $\bczt>0$ is a UCB style bias parameter.

    Let $\relevant(s,a;\bff{w})$ denote the set of \textit{relevant} balls at a decision node $s$ for action $a$. A ball is relevant if $(\bff{w},a)$ is in the domain of the ball. Specifically,
    \begin{align}
        \relevant(s,a;\bff{w}) = \{ B \in A(s,a) \mid (\bff{w},a)\in\dom(B)\}, \\
        \relevant(s;\bff{w}) = \cup_{a\in\cl{A}} \relevant(s,a;\bff{w}).
    \end{align}

    Contextual Zooming defines a confidence interval term, called the \textit{pre-index}, around each ball, similar to UCB's confidence interval term:
    \begin{align}
        \Ipreczt(B;\bff{w}) = \bff{w}^\top \Qczt(B) + r(B) + \conf(B). \label{eq:5:cz_pre_index}
    \end{align}

    The full \textit{index} of a ball $B$ allows a newly activated ball with few visits to inherit an optimistic value estimate from nearby well-explored balls, ensuring it remains eligible for selection. Specifically,
    \begin{align}
        \Iczt(B;\bff{w}) = 
            r(B) + \min_{B'\in A(s)} \left( \Ipreczt(B';\bff{w}) + d(B,B') \right), \label{eq:5:cz_index}
    \end{align}

    where $d(B,B')$ is the distance between the centres of balls $B$ and $B'$ according to the metric $d$. In CZT the constant $\Cczt$ is set to a large enough value such that balls with a different action will never minimise the expression, so it suffices to consider only balls with the same action as $B$ in Equation (\ref{eq:5:cz_index}).

    To select a ball, the \selectball\ewe function chooses the ball from the set of relevant balls with the highest index:
    \begin{align}
        \selectball(s,\bff{w}) = \argmax_{B\in\relevant(s;\bff{w})} \Iczt(B).
    \end{align}

    If $\selectball(s,\bff{w}) = \ball((\bff{w}_B,a_B),r_B)$, then the CZT selection policy returns $\piczt(s;\bff{w})=a_B$.

\paragraph{Backups and Activation Rule}

    Let $\bff{\tau}_{:h}\sim\piczt$ be the trajectory sampled in the selection phase of CZT. Let $B_t$ be the ball selected at the decision node $s_t$. 

    The backup functions update the ball selection counts and the multi-objective average returns for $t=h-1,\ldots,0$:
    \begin{align}
        M(B_t) &\leftarrow M(B_t) + 1, \\
        \Qczt(B_t) &\leftarrow \frac{M(B_t)-1}{M(B_t)} \Qczt(B_t) + \frac{\Vinitbff(s_h) + \sum_{i=t}^{h-1} \bff{r}_i}{M(B_t)},
    \end{align}
    where $\Vinitbff(s_h)$ is the (multi-objective) heuristic value function.

    The \textit{activation rule} introduces a new ball with smaller radius when the following conditions are met:
    \begin{align}
        M(B_t) > \Mmin \quad \text{and} \quad \conf(B_t) \leq r(B_t),
    \end{align}
    %
    where $\Mmin$ is a minimum selection threshold, and when both conditions are met, a new ball $B_{\textnormal{new}} = \ball((\bff{w},a_t),\frac{r(B_t)}{2})$ is added to the set of active balls $A(s,a)$.

\paragraph{Recommendation Policy}

    To recommend an action in CZT, the ball with maximum linear utility from the set of active balls is selected. Specifically,
    \begin{align}
        \psiczt(s;\bff{w}) = \argmax_{a\in\cl{A}} \max_{B\in \relevant(s,a;\bff{w})} \bff{w}^\top \Qczt(B).
    \end{align}


\subsection{Differences from Contextual Zooming}
\label{sec:5-2-2-cz-diffs}

    Section \ref{sec:5-2-1-czt} described the CZT algorithm. The main adaptations from CZ are summarised below:
    \begin{enumerate}
        \item 
            In CZT, the set of active balls is specialised to a set for each action. In CZ, a single set of active balls is used across the entire \textit{similarity space} (defined in Section \ref{sec:2-1-3-contextual-mab}).
        \item \label{item:5:local1}
            CZ assumes that the algorithm is run for a known number of rounds $T$, and uses this value instead of the current number of visits at the decision node $N(s)$ in the confidence radii (Equation \ref{eq:5:cz_conf_radius}).
        \item 
            The activation rule of CZ does not have a hard lower bound on the number of ball visits required to add a new child ball. CZT makes this adaptation to account for item \ref{item:5:local1}, which results in smaller confidence radii.
        \item 
            In CZT, all balls contain exactly one action, whereas in CZ with a general continuous space of arms, balls cover many arms. After a ball is selected in CZ, an arm is sampled uniformly randomly from the domain of the selected ball. 
    \end{enumerate}

    CZ \cite{cz,cz_long} extends its regret analysis from a fixed number of rounds $T$ to an unknown number of rounds using the \textit{doubling trick} \cite{doubling_trick}. The doubling trick runs an independent instance of the algorithm for $T$ rounds, then $2T$ rounds, then $4T$ rounds and so on. In practice, however, discarding all accumulated data at each doubling interval is undesirable. Instead, in CZT the number of visits $N(s)$ at the current node is used directly in place of $T$, which avoids the need for the doubling trick at the cost of losing regret guarantees.
    












% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\section{Convex Hull Monte Carlo Tree Search}
\label{sec:5-3-chmcts}

    This section introduces \textit{Convex Hull Monte Carlo Tree Search} (CHMCTS), which generalises prior work in MOMCTS \ref{sec:3-5-momcts} to stochastic environments by maintaining \textit{convex hull value sets} at each node in the search tree and updating them using convex hull backups (Section \ref{sec:2-6-1-chvi}). When the MOMDP is deterministic and a search policy from prior work is used, CHMCTS reduces to the prior MOMCTS algorithms discussed in Chapter \ref{ch:3-relatedwork}.

    The common \thtspp\ewe framework and backup equations shared by all CHMCTS algorithms are given first. The search policies for each algorithm are then defined. Two practical optimisations are discussed in Section \ref{sec:5-3-6-optimisations}, and the section concludes with a brief discussion on the relationship between entropy and convex hull value sets.

% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\subsection{Common Framework}
\label{sec:5-3-1-framework}

    Let $\alg$ denote any of the CHMCTS algorithms defined in this section. At each decision node $s$, a convex hull value set $\Valg(s)$ is maintained, and at each chance node $(s,a)$, a convex hull Q-value set $\Qalg(s,a)$ is maintained. The specific form of the search policy varies between algorithms and is defined in the subsections below.

    Given a context weight $\bff{w}\in\Delta^D$ and a trajectory $\tau_{:h}=(s_0,a_0,\bff{r}_0,\ldots,s_{h-1},a_{h-1},\bff{r}_{h-1},s_h)\sim\pialg(\cdot|\cdot;\bff{w})$, the value set estimates are updated for $t=h-1,\ldots,0$:
    \begin{align}
        \Valg(s_t) &\leftarrow \cprune \left( 
            \bigcup_{a\in\cl{A}} \Qalg(s_t,a) \right), 
            \label{eq:5:chmcts_v_backup} \\
        \Qalg(s_t,a_t) &\leftarrow \bff{R}(s_t,a_t) + 
            \sum_{s'\in\suc{s_t}{a_t}} 
            \frac{N(s')}{N(s_t,a_t)}\Valg(s'). 
            \label{eq:5:chmcts_q_backup}
    \end{align}

    The $\cprune$ operation, defined in Equation 
    (\ref{eq:2:cprune_def}) and repeated here for convenience, 
    retains only those vectors in a set $\bfcl{V}$ that are 
    optimal for at least one weight vector:
    \begin{align}
        \cprune(\bfcl{V}) = \{\bff{v}\in\bfcl{V} \mid 
            \exists \bff{w}\in\Delta^D.\ 
            \forall \bff{v}'\in\bfcl{V}-\{\bff{v}\}.\ 
            \bff{w}^\top \bff{v} > \bff{w}^\top \bff{v}' \}. 
            \label{eq:5:cprune_def}
    \end{align}

    Since the context weight is passed to policies, it is useful to define \textit{contextual value estimates}, which extract a scalar value from a convex hull value set for a given weight $\bff{w}\in\Delta^D$:
    \begin{align}
        \Valgctx{\bff{w}}(s) &= \max_{\bff{v}\in\Valg(s)} 
            \bff{w}^\top \bff{v}, 
            \label{eq:5:contextual_value} \\
        \Qalgctx{\bff{w}}(s,a) &= \max_{\bff{q}\in\Qalg(s,a)} 
            \bff{w}^\top \bff{q}. 
            \label{eq:5:contextual_qvalue}
    \end{align}

    The recommendation policy selects the action with the highest contextual Q-value under the given weight:
    \begin{align}
        \psialg(s;\bff{w}) = \argmax_{a\in\cl{A}} 
            \Qalgctx{\bff{w}}(s,a). \label{eq:5:chmcts_rec}
    \end{align}

% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\subsection{Convex Hull UCT}
\label{sec:5-3-2-chuct}

    Another natural extension of UCT to the contextual multi-objective setting is to replace the scalar Q-value estimates (used in Section \ref{sec:2-4-2-uct}) with contextual value estimates from Equations (\ref{eq:5:contextual_value}) and (\ref{eq:5:contextual_qvalue}). The resulting algorithm, \textit{Convex Hull UCT} (CH-UCT), uses the following search policy:
    \begin{align}
        \pichuct(s;\bff{w}) = \argmax_{a\in\cl{A}} \left[
            \Qchuctctx{\bff{w}}(s,a) + \cchuct 
            \sqrt{\frac{\log(N(s))}{N(s,a)}} \right],
            \label{eq:5:chuct_policy}
    \end{align}
    where $\cchuct > 0$ is a bias parameter controlling exploration. The search policy is context-dependent, since the contextual Q-value $\Qchuctctx{\bff{w}}(s,a)$ depends on $\bff{w}$, ensuring that action selection is consistent with the sampled objective throughout each trial.

% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\subsection{Convex Hull BTS}
\label{sec:5-3-3-chbts}

    \textit{Convex Hull BTS} (CH-BTS) uses the BTS search policy (Section \ref{sec:4-2-1-bts}) within the CHMCTS framework, using contextual value estimates in place of the scalar Bellman value estimates:
    \begin{align}
        \rhochbts(a|s;\bff{w}) &\propto \exp\left(
            \frac{\Qchbtsctx{\bff{w}}(s,a)}
            {\alphachbts(N(s))}\right), 
            \label{eq:5:chbts_rho} \\
        \pichbts(a|s;\bff{w}) &= (1-\lambdachbts)
            \rhochbts(a|s;\bff{w}) 
            + \frac{\lambdachbts}{|\cl{A}|}, 
            \label{eq:5:chbts_policy} \\
        \lambda(s,\epsilon) &= \min\left(1, 
            \frac{\epsilon}{\log(e+N(s))}\right),
    \end{align}
    where $\alphachbts(x) > 0$ is a temperature function and $\epschbts \in [0,\infty)$ is an exploration parameter. As in BTS, the temperature function controls the trade-off between exploration and exploitation, and the exploration parameter ensures that all actions are eventually explored.

% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\subsection{Convex Hull CZT}
\label{sec:5-3-4-chczt}

    In single-objective MCTS, Bellman backups often outperform average return backups in the exploration setting (Chapter \ref{ch:4-dents}), since they propagate value information more directly through the tree by bootstrapping estimates from successor states rather than averaging over full trajectories. Convex hull 
    backups can be viewed as the multi-objective analogue of 
    Bellman backups: rather than maintaining a scalar average 
    return at each node, they maintain a set of achievable 
    value vectors that more accurately represents the range 
    of outcomes reachable from that node. The CZT ball value 
    estimates $\Qczt(B)$ provide a coarser representation, 
    averaging over a region of weight space rather than 
    capturing the full structure of achievable values.

    Since the CHMCTS backups (Equations 
    (\ref{eq:5:chmcts_v_backup}) and 
    (\ref{eq:5:chmcts_q_backup})) are independent of the 
    search policy, they can be combined with any search 
    policy, including CZT. The resulting algorithm, 
    \textit{Convex Hull CZT} (CH-CZT), runs the CZT 
    algorithm but maintains additional convex hull value 
    sets at each node and uses the recommendation policy 
    from Equation (\ref{eq:5:chmcts_rec}) in place of the 
    CZT recommendation policy. This combines the contextual 
    exploration of CZT with the richer value representation 
    of CHMCTS.

% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\subsection{Prior Search Policies}
\label{sec:5-3-5-prior}

    The MOMCTS algorithms discussed in Chapter \ref{ch:3-litrev} can be recovered within the CHMCTS framework by substituting appropriate search policies. In general, prior work uses search policies of the following form:
    \begin{align}
        \pialg(a|s;\bff{w}) = \argmax_{a\in\cl{A}} \left[
            \zetaalg(\Qalg(s,a);\bff{w}) + \calg 
            \sqrt{\frac{\log(N(s))}{N(s,a)}} \right],
            \label{eq:5:prior_policy}
    \end{align}
    where $\zetaalg$ is a scalarisation function that maps a 
    convex hull Q-value set to a scalar, and $\calg > 0$ is 
    a bias parameter. Notably, when $\zetaalg$ does not depend 
    on $\bff{w}$, the search policy is context-independent, 
    which as discussed in Section \ref{sec:5-1-intro} leads to 
    linear contextual cumulative regret.
 

\paragraph{Hypervolume CHMCTS (H-CHMCTS).}

    \citet{hv_momcts_1} use the \textit{hypervolume indicator} (Definition \ref{def:2:hypervolume}) as the scalarisation function $\zetaalg$ in the search policy. That is, $\zetahchmcts(\bfcl{V};\bff{w}) = \HV(\bfcl{V};\rref)$, where $\rref$ is reference point for the hypervolume indicator and is a parameter of the algorithm.
    
\paragraph{Chebyshev CHMCTS (C-CHMCTS).}

    \citet{chebyshev_momcts} use the \textit{Chebyshev utility}:

    \begin{defn}
        \label{def:5:chebyshev_utility}
        Let $\bff{v},\ruto\in\bb{R}^D$ and $\bff{w}\in\Delta^D$. The \textnormal{Chebyshev utility} of $\bff{v}$ with respect to $\bff{w}$ and $\ruto$ is:
        \begin{align}
            \ucheby(\bff{v};\bff{w},\ruto) = - \max_{i=1,\ldots,D} w_i |v_i - \rutocomp_i|.
        \end{align}
    \end{defn}

    The scalarisation function for C-CHMCTS is:
    \begin{align}
        \zetcchmcts(\bfcl{V};\bff{w},\ruto) = \max_{\bff{v}\in\bfcl{V}} \ucheby(\bff{v};\bff{w},\ruto),
    \end{align}
    %
    where $\ruto$ is a \textit{utopian point} satisfying $\ruto \succeq_P \bff{v}$ for all $\bff{v}\in\bfcl{V}$ and is a parameter of the algorithm.

\paragraph{Pareto CHMCTS (P-CHMCTS).}

    The notable exception to this form is Pareto MCTS \cite{pareto_momcts}, which uses a Pareto UCB algorithm \cite{pareto_ucb,pareto_ucb_2} at each decision node, and does not fit directly into the template of Equation (\ref{eq:5:prior_policy}). \textit{Pareto CHMCTS} (P-CHMCTS) is the algorithm that results from running Pareto UCB at each decision node within the CHMCTS framework.

    Pareto UCB adds a UCB-style bonus to each vector in the value set:
    \begin{align}
        \Vparetoconf(s) &= \cprune\left( \Vparetou(s) \right), \\
        \Vparetou(s) &= \left\{ 
            \left(\bff{q} + U_{\textnormal{pucb}}(s,a)\bff{1}, a \right)
            \mid a \in \cl{A}, \bff{q} \in \Qpareto(s,a)
        \right\}, \\
        U_{\textnormal{pucb}}(s,a) &= \bpareto \sqrt{\frac{\log(N(s))}{N(s,a)}}.
    \end{align}
    %
    where $\bpareto > 0$ is a bias parameter and where the notation $(\bff{q}, a)$ tags each vector with its corresponding action to allow the action to be recovered after the $\cprune$ operation.

    The search policy samples a point uniformly randomly from the \textit{confidence value set}, and selects the corresponding action. That is:
    \begin{align}
        \pipareto(a|s;\bff{w}) =  \sum_{(\bff{v},a')\in\Vparetoconf(s)} \frac{\one[a=a']}{|\Vparetoconf(s)|}.
    \end{align}


% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\subsection{Optimisations}
\label{sec:5-3-6-optimisations}

    Two practical optimisations are used in the implementation of CHMCTS: \textit{solved labelling}, which avoids wasting computation on well-explored parts of the search tree; and \textit{size pruning}, which limits the number of points stored in each convex hull value set.

\paragraph{Solved Labelling.}

    In planning, \textit{labelling} \cite{lrtdp} is used to mark nodes as solved and prune them from further consideration, avoiding wasted computation on parts of the tree that are already solved. A node is marked as solved when its value estimate has converged to the true value, which requires knowledge of the true transition distribution. In the setting considered here, the true transition distribution $p(\cdot|s,a)$ is unknown, so a probabilistic analogue for labelling is used instead.

    Each chance node $(s,a)$ is assigned an \textit{unsolved value} $\unsolved(s,a)\in[0,1]$, where $\unsolved(s,a)=0$ indicates that the node is solved and $\unsolved(s,a)=1$ indicates that it is unsolved. The solved value of a node is an upper bound on the probability that the node's value estimate has not yet converged.

    The unsolved value of a decision node is computed as the maximum unsolved value of its children:
    \begin{align}
        \unsolved(s) = \max_{a\in\cl{A}} \unsolved(s,a).
    \end{align}

    The unsolved values are updated during the backpropagation phase of \thtspp\ewe and used during the selection phase to filter out actions that have already been solved, to a tolerance of $\unsolvedtol$, with high probability. That is, at a decision node $s$, any action $a$ with $\unsolved(s,a) \leq \unsolvedtol$ is filtered out before selecting an action. The remainder of this section describes how the unsolved values are propagated through chance nodes, that is, how the value of $\unsolved(s,a)$ is computed.

    % Ideally $\unsolved(s,a)$ would be computed either as an expectation or maximisation over the successor states. Using the empirical distribution directly may be overly optimistic when few successor states have been observed, and using a maximisation over successor states penalises nodes with low-probability transitions that are rarely visited. An upper bound on the expected unsolved value is used instead, which accounts for both the estimation error in the empirical distribution and the probability mass of unobserved successor states.

    The solved value of a chance node $(s,a)$ is computed as an upper bound on the expected solved values of its successor states, using the empirical transition distribution $\frac{N(s')}{N(s,a)}$ as a proxy for the true distribution $p(s'|s,a)$. Since the true distribution is unknown, the bound must account for the estimation error in the empirical distribution. Let $\deltaunsolved > 0$ be a confidence parameter. The unsolved value $\unsolved(s,a)$ is defined as an upper bound satisfying:
    \begin{align}
        \Pr\left(\bb{E}_{s'\sim p(\cdot|s,a)}[\unsolved(s')] \leq \unsolved(s,a)\right) \geq 1-\deltaunsolved. \label{eq:5:unsolved_chance_defn}
    \end{align}

    In words, with probability at least $1-\deltaunsolved$, the expected solved value of the children of chance node $(s,a)$ is bounded by the upper bound $\unsolved(s,a)$.

    To derive this bound, the expected unsolved value is decomposed into contributions from observed and unobserved successor states:
    \begin{align}
        \mathbb{E}[\unsolved(s')] 
            =& \sum_{s'\in\sucobs{s}{a}} 
                p(s'|s,a)\unsolved(s') \label{eq:5:local6} \\
            & + \sum_{s'\notin\sucobs{s}{a}} 
                p(s'|s,a)\unsolved(s'), \label{eq:5:local7}
    \end{align}
    where $\sucobs{s}{a}= \{s'\in\suc{s}{a} \mid N(s') > 0\}$ is the set of successor states observed so far.

    For the observed successors, Corollary \ref{cor:4:bound_transition_distribution} of the DKW inequality (Theorem \ref{thrm:2:dkw_inequality}) gives with probability at least $1-\frac{\deltaunsolved}{2}$ for all $s'\in\sucobs{s}{a}$:
    \begin{align}
        p(s'|s,a) \leq \frac{N(s')}{N(s,a)} + 
            2\sqrt{\frac{\log(4/\deltaunsolved)}{2N(s,a)}},
    \end{align}
    so the contribution of observed successors in (\ref{eq:5:local6}) is bounded by:
    \begin{align}
        &\sum_{s'\in\sucobs{s}{a}} 
            p(s'|s,a)\unsolved(s') \\
        \leq & \sum_{s'\in\sucobs{s}{a}} 
            \left(\frac{N(s')}{N(s,a)} + 
            2\sqrt{\frac{\log(4/\deltaunsolved)}{2N(s,a)}}\right)\unsolved(s').
    \end{align}

    The \textit{Good-Turing estimate} (Section \ref{sec:2-5-convergence}) for the missing mass is:
    \begin{align}
        \goodturingestimate(s,a) 
            = \sum_{s'\in\suc{s}{a}}\frac{\one\left[N(s')=1\right]}{N(s,a)}.
    \end{align}

    For the unobserved successors, since $\unsolved(s')\leq 1$ for all $s'$, the contribution of unobserved successors (\ref{eq:5:local7}) is bounded by the missing mass using Theorem \ref{thrm:2:good_turing_bound} with probability at least $1-\frac{\deltaunsolved}{2}$:
    \begin{align}
        \sum_{s'\not\in\sucobs{s}{a}} p(s'|s,a) 
        \leq \goodturingestimate(s,a) + \sqrt{\frac{\log(2/\deltaunsolved)}{N(s,a)}}.
    \end{align}
    
    Combining these bounds via the union bound, the following assignment of $\unsolved(s,a)$ satisfies (\ref{eq:5:unsolved_chance_defn}):
    \begin{align}
        \unsolved(s,a) \leftarrow &
            \sum_{s'\in\sucobs{s}{a}} 
            \left(\frac{N(s')}{N(s,a)} + 
            2\sqrt{\frac{\log(4/\deltaunsolved)}{2N(s,a)}}\right)\unsolved(s') \\
            + &\ \goodturingestimate(s,a) + \sqrt{\frac{\log(2/\deltaunsolved)}{N(s,a)}}.
            \label{eq:5:solved_value}
    \end{align}
    
    When the $\unsolved$ labelling is used in CHMCTS, $\unsolvedtol$ and $\deltaunsolved$ are hyperparameters of the algorithm. A node is marked as solved when $\unsolved(s,a) \leq \unsolvedtol$, at which point it is excluded from further trials.

\paragraph{Convex Hull Size Pruning.}

    As the search progresses, the convex hull value sets at each node may grow large, increasing the computational cost of the $\cprune$ operation and the convex hull backups. To limit this cost, a maximum size $K_{\max}$ is imposed on each convex hull value set. When a value set exceeds this limit after a backup, points are pruned using the \textit{crowding distance} from the NSGA-II algorithm \cite{nsga_ii}

    The crowding distance of a point $\bff{v}$ in a set $\bfcl{V}$ measures how isolated $\bff{v}$ is from its neighbours in objective space. For each objective dimension $i=1,\ldots,D$, the points in $\bfcl{V}$ are sorted by their $i$th coordinate. The boundary points (those with the smallest and largest values in dimension $i$) are assigned an infinite crowding distance and are always retained. For interior points, the crowding distance contribution from dimension $i$ is:
    \begin{align}
        \textnormal{cd}_i(\bff{v}) = 
            \frac{v^+_i - v^-_i}{\max_{\bff{v}'\in\bfcl{V}} 
            v'_i - \min_{\bff{v}'\in\bfcl{V}} v'_i},
    \end{align}
    where $v^+_i$ and $v^-_i$ are the $i$th coordinates of the nearest neighbours of $\bff{v}$ above and below in dimension $i$ respectively. The total crowding distance is $\textnormal{cd}(\bff{v}) = \sum_{i=1}^D \textnormal{cd}_i(\bff{v})$. When the value set exceeds $K_{\max}$ points, the $K_{\max}$ points with the largest crowding distances are retained, preserving the spread of the convex hull.


% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\subsection{Entropy and Convex Hulls}
\label{sec:5-3-7-entropy-convex-hulls}

    \begin{figure}
        \centering
        \begin{subfigure}[b]{0.49\textwidth}
            \centering
            \includegraphics[width=0.8\textwidth]{figures/ch5/ch_and_entropy_a.pdf}
            \label{fig:5:entropy_convex_hull_a}
        \end{subfigure}
        \begin{subfigure}[b]{0.49\textwidth}
            \centering
            \includegraphics[width=0.8\textwidth]{figures/ch5/ch_and_entropy_b.pdf}
            \label{fig:5:entropy_convex_hull_b}
        \end{subfigure}
        \caption[Entropy and convex hull value sets.]{A convex hull value set where a single point is optimal for two different weight vectors $\bff{w}$ and $\bff{w}'$. Although both weight vectors select the same value at the root state (a), they induce different policies with different values at downstream states (b), resulting in different entropy values.}
        \label{fig:5:entropy_convex_hull}
    \end{figure}

    Convex hull value sets represent the set of achievable expected returns in \textit{objective space}, storing value vectors attainable by different policies. Entropy, by contrast, is a property of a policy rather than a value vector: it measures the uncertainty of the action distribution induced by a policy at each state.

    A key observation is that a single point on the convex hull does not correspond to a unique policy, as illustrated in Figure \ref{fig:5:entropy_convex_hull}. Since the contextual policies considered in this chapter depend on the weight vector $\bff{w}$, a given value vector $\bff{v}\in\Valg(s)$ may be optimal for many different weight vectors, each inducing a different policy $\pi(\cdot|s;\bff{w})$ with a different entropy. 

    As a result, there is no principled way to associate a single entropy value with a point on the convex hull. This is the fundamental reason why the entropy-based exploration used in DENTS (Chapter \ref{ch:4-dents}) does not extend naturally to the multi-objective setting with convex hull value sets, and motivates the simplex map approach introduced in Chapter \ref{ch:6-simplexmaps}.










    














% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\section{Empirical Results}
\label{sec:5-5-results}

    This section presents the empirical results for this chapter. It is structured similarly to Section \ref{sec:4-4-results} in Chapter \ref{ch:4-dents}, as follows:
    \begin{itemize}
        \item Section \ref{sec:5-5-1-envs} describes the MOMDP environments used in the experiments;
        \item Section \ref{sec:5-5-2-eval_procedure} describes the evaluation setup used;
        \item Section \ref{sec:5-5-3-results_and_discussion} presents the results on the MOMDP environments;
        \item Section \ref{sec:5-5-4-scalability} presents scalability results on the \textit{Deep Sea Treasure} environment;
        \item Appendix \ref{app:expr-deets-ch5} provides the hyperparameters used and how they were chosen.
    \end{itemize}

    The implementation of Convex Hull based algorithms used the QHull library \cite{qhull} for convex hull operations where possible, falling back to the linear programming method to compute $\cprune$ (Equation \ref{eq:2:cprune_def}) of \cite{cccs} for geometrically degenerate cases.






% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\subsection{Environments}
\label{sec:5-5-1-envs}

    Standard gridworld environments from \textit{MO-Gymnasium} \cite{mo_gym} are used to investigate the behaviour of the search algorithms. 

    \begin{description}
        \item[Resource-Gathering] 
            An agent explores a gridworld, and aims to collect a piece of gold and a diamond and return to the starting location. There are two enemies on the map, which will attack and kill the agent with a probability of $\frac{1}{10}$ when the agent enters the enemy's cell. If killed the agent is reset to the starting location. There are three rewards, a cost of $-1$ for being killed, a reward of $1$ for returning with the goal, and a reward of $1$ for returning with the diamond.
        \item[Four-Room] 
            The agent must navigate from a starting location to a goal location, with obstructing walls in the map. There are three rewards, corresponding to three collectibles contained in the map.
        \item[Fruit-Tree-7]
            An MOMDP consisting of a binary tree of depth 7, where each leaf node is a terminal state giving a random $6$-dimensional reward vector. 
        \item[Breakable-Bottles]
            A $1$-dimensional gridworld, where the agent can move between the home state and a bottle bank. The agent can pick up bottles, and carry at most two bottles at a time. If the agent is carrying two bottles at once, there is a probability of $\frac{1}{10}$ that one of the bottles is dropped. Once a bottle is dropped, it cannot be picked up again. There are three rewards, a time cost of $-1$ per timestep, a reward of $25$ for delivering a bottle and an additional cost of $-1$ for dropping a bottle. The trial ends when the agent has delivered two bottles.
    \end{description}

    For each environment, a horizon of $H=100$ was used where applicable.



    \paragraph{Deep Sea Treasure}

    \begin{figure}
        \centering
        \includegraphics[width=0.3\textwidth]{figures/ch5/dst.png}
        \caption[Deep Sea Treasure environment.]{The Deep Sea Treasure environment, $\DST(10,\pswept)$. Illustration is via \cite{improved_dst}.}
        \label{fig:5:dst_env}
    \end{figure}

    In \textit{Deep Sea Treasure} (DST) environment, illustrated in Figure \ref{fig:5:dst_env}, the agent starts in the top left corner and can move in any cardinal direction. DST has two rewards: first a time cost of $-1$ per step and second a \textit{treasure} reward for reaching one of the goal states where the magnitude of the reward is specified by the map.

    While MO-Gymnasium provides a \textit{Deep Sea Treasure} environment, a custom \texttt{C++} implementation of \textit{Deep Sea Treasure} is used for the following reasons:
    \begin{itemize}
        \item provides a more efficient implementation, enabling the scalability experiments in Section \ref{sec:5-5-4-scalability};
        \item provides access to a list of states and transition probabilities between states, so that CHVI (Section \ref{sec:2-6-1-chvi}) can be used as a baseline;
        \item allows the transition model to include stochasticity.
    \end{itemize} 

    To introduce a stochastic component to DST, there is a $\pswept$ probability of the agent being swept by a current at each timestep. When swept, the agent is moved in an additional random direction before the agent makes their own move.

    To enable scalability experiments, a range of map sizes are generated using the MORLGlue library \cite{morl_glue}, parameterised by the width of the gridworld. Let $\DST(W,\pswept)$ denote a DST environment with width $W$ and probability of being swept $\pswept$. For each map of width $W$, if $(x_{\text{treasure}},y_{\text{treasure}})$ is the coordinate of furthest treasure, then the maximum treasure value and planning horizon are both set to $2\times (x_{\text{treasure}}+y_{\text{treasure}})$, so that both rewards are given a consistent scale.

    In Section \ref{sec:5-5-3-results_and_discussion} the $\DST(10,0)$, $\DST(10,\frac{1}{40})$ and $\DST(10,\frac{1}{5})$ environments are used. In Section \ref{sec:5-5-4-scalability} the environments $\DST(W,0)$, $\DST(W,\frac{1}{40})$ and $\DST(W,\frac{1}{5})$ are used, with $W$ ranging from $10$ to $100$.








\subsection{Evaluation Procedure} 
\label{sec:5-5-2-eval_procedure}

    It is worth noting that in multi-objective MCTS, the 
    quality of the convex hull at the root node is the 
    primary concern. Even when a multi-objective MCTS 
    algorithm is used in an online setting, where tree 
    search is interleaved with actions taken by the agent, 
    once a policy is selected at the initial state it 
    implicitly commits to a weighting over the objectives 
    (Section \ref{sec:5-1-intro}). A more efficient online 
    approach is therefore to run the multi-objective tree 
    search from the initial state, and subsequently follow 
    a single-objective MCTS algorithm in the scalarised MDP 
    with reward $R(s,a;\bff{w}) = \bff{w}^\top \bff{R}(s,a)$,
    where $\bff{w}$ is inferred from the choice made at the root node.

    To evaluate the quality of the convex hull at the root node, the \textit{Expected Utility Metric} (Definition \ref{def:2:eum}) and the \textit{Hypervolume} (Definition \ref{def:2:hypervolume}) are used.

    Given a context $\bff{w}$ and search tree $\cl{T}$ that defines a partial recommendation policy $\psi_{\alg}$, the policy will be made complete similarly to Section \ref{sec:4-4-2-eval_procedure}:
    \begin{align}
        \texttt{complete}(\psi_{\alg})(a|s;\bff{w}) =
        \begin{cases}
            \psi_{\alg}(a|s;\bff{w})                       & \text{ if } s\in\cl{T}, \\
            \frac{1}{|\cl{A}|}      & \text{ otherwise.}
        \end{cases} \label{eq:5:full_Recommend}
    \end{align}
    % 

    The Convex Hull (of policies) can then be written as 
    \begin{align}
        \Psi_{\alg} = \{\texttt{complete}(\psi_{\alg})\ |\ \bff{w}\in\Delta^D \}.
    \end{align}

    A Monte Carlo estimate of $\eum(\Psi_{\alg})$ (Definition \ref{def:2:eum}) is computed. Following \cite{well_spaced_points}, let $E(\Delta^D)$ be an evenly spaced set of points in $\Delta^D$. Then the \eum\ewe can be approximated as:
    \begin{align}
        \eum(\Psi_{\alg}) \approx \sum_{\bff{w}\in E(\Delta^D)} \bff{w}^\top \bff{\hat{V}}^{\bff{\tau}(\bff{w})},
    \end{align}
    %
    where $\bff{\tau}(\bff{w}) \sim \texttt{complete}(\psi_{\alg})$ is a sampled trajectory for the point $\bff{w}\in\Delta^D$, and $\bff{\hat{V}}^{\bff{\tau}(\bff{w})}$ is the cumulative return from the trajectory. That is, if the trajectory is $\bff{\tau}(\bff{w})=(s_0,a_0,\bff{r}_0,...,s_{h-1},a_{h-1},\bff{r}_{h-1},s_h)$, then $\bff{\hat{V}}^{\bff{\tau}(\bff{w})} = \sum_{i=0}^{h-1} \bff{r}_i$.

    Each algorithm is given $60$ seconds of search time, except for DST environments where $15$ seconds are given. Each algorithm uses $16$ threads for computation and value estimates for the EUM are computed using $128$ Monte Carlo samples, i.e. $|E(\Delta^D)| = 128$.

    As in Section \ref{sec:4-4-results}, the BayesOpt package \cite{bayesopt} is used to tune the hyperparameters of all algorithms. Full details on the hyperparameter tuning of multi-objective algorithms is given in Appendix \ref{app:expr-deets-ch5}.




\subsection{Multi-Objective Gridworld Results}
\label{sec:5-5-3-results_and_discussion}

    For CZT, to compute a hypervolume, an approximate convex hull is computed, using the $\Qczt$ value estimates at each node.

    \begin{figure}
        \centering
        % Row 1
        \begin{subfigure}[b]{0.4\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/520_gym_resource_gather_ch5_0_eum.png}
            \caption{Resource-Gathering (EUM).}
            \label{fig:5:gridworld_results:sub:a}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.4\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/520_gym_resource_gather_ch5_2_hypervolume.png}
            \caption{Resource-Gathering (HV).}
            \label{fig:5:gridworld_results:sub:b}
        \end{subfigure}

        \vspace{1em}

        % Row 2
        \begin{subfigure}[b]{0.4\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/550_four_room_ch5_0_eum.png}
            \caption{Four-Room (EUM).}
            \label{fig:5:gridworld_results:sub:c}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.4\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/550_four_room_ch5_2_hypervolume.png}
            \caption{Four-Room (HV).}
            \label{fig:5:gridworld_results:sub:d}
        \end{subfigure}

        \vspace{1em}

        % Row 3
        \begin{subfigure}[b]{0.4\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/500_fruit_tree_ch5_0_eum.png}
            \caption{Fruit-Tree-7 (EUM).}
            \label{fig:5:gridworld_results:sub:e}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.4\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/500_fruit_tree_ch5_2_hypervolume.png}
            \caption{Fruit-Tree-7 (HV).}
            \label{fig:5:gridworld_results:sub:f}
        \end{subfigure}

        \vspace{1em}

        % Row 4
        \begin{subfigure}[b]{0.4\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/540_breakable_bottles_ch5_0_eum.png}
            \caption{Breakable-Bottles (EUM).}
            \label{fig:5:gridworld_results:sub:g}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.4\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/540_breakable_bottles_ch5_2_hypervolume.png}
            \caption{Breakable-Bottles (HV).}
            \label{fig:5:gridworld_results:sub:h}
        \end{subfigure}

        \caption[Results on MO-Gymnasium environments.]{Results on the MO-Gymnasium environments. 
        (Left) Expected Utility Metric (EUM). (Right) Hypervolume 
        (HV). Each row shows the results for a different 
        environment: Resource-Gathering, Four-Room, Fruit-Tree-7 
        and Breakable-Bottles respectively.}
        \label{fig:5:gridworld_results}
    \end{figure}

    \paragraph{Gymnasium Environments.}

    Results on the MO-Gymnasium environments are given in 
    Figure \ref{fig:5:gridworld_results}. On all four 
    environments, algorithms using convex hull backups 
    generally outperformed CZT. This is consistent with 
    the exploitation bias of UCT-based algorithms discussed in Chapter 
    \ref{ch:4-dents}, where UCT-style algorithms tend to 
    converge to a local optimum.

    On the Resource-Gathering and Breakable-Bottles 
    environments, CH-UCT and CH-BTS clearly outperform 
    all other algorithms on both EUM and hypervolume 
    metrics. These environments have larger state spaces 
    and more complex reward structures, where the benefit 
    of contextual search policies is most pronounced.

    On Four-Room and Fruit-Tree-7, contextual search 
    policies did not clearly outperform context-independent 
    ones. Fruit-Tree-7 is a small environment where the 
    difference between algorithms is less significant. 
    Four-Room is larger, 
    but its deterministic nature and lack of a time cost 
    reward means that a single path collecting all 
    collectibles is sufficient to achieve a near-optimal 
    solution for any weight vector, enabling a 
    context-independent policy to perform comparably to 
    a contextual one.
    P-CHMCTS, despite using a contextual 
    search policy, did not clearly outperform 
    context-independent policies on any of the 
    environments.











    \begin{figure}
        \centering
        % Row 1
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/400_dst_ch5_0_eum.png}
            \caption{$\DST(10,0)$ (EUM).}
            \label{fig:5:dst_results:sub:a}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/400_dst_ch5_2_hypervolume.png}
            \caption{$\DST(10,0)$ (HV).}
            \label{fig:5:dst_results:sub:b}
        \end{subfigure}

        \vspace{1em}

        % Row 2
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/420_dst_calm_stoch_ch5_0_eum.png}
            \caption{$\DST(10,\frac{1}{40})$ (EUM).}
            \label{fig:5:dst_results:sub:c}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/420_dst_calm_stoch_ch5_2_hypervolume.png}
            \caption{$\DST(10,\frac{1}{40})$ (HV).}
            \label{fig:5:dst_results:sub:d}
        \end{subfigure}

        \vspace{1em}

        % Row 3
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/410_dst_stoch_ch5_0_eum.png}
            \caption{$\DST(10,\frac{1}{5})$ (EUM).}
            \label{fig:5:dst_results:sub:e}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/410_dst_stoch_ch5_2_hypervolume.png}
            \caption{$\DST(10,\frac{1}{5})$ (HV).}
            \label{fig:5:dst_results:sub:f}
        \end{subfigure}

        \caption[Results on Deep Sea Treasure environments.]{Results on the Deep Sea Treasure 
        environments. (Left) Expected Utility Metric (EUM). 
        (Right) Hypervolume (HV). Each row shows the results for 
        $\DST(10,0)$, $\DST(10,\frac{1}{40})$ and 
        $\DST(10,\frac{1}{5})$ respectively.}
        \label{fig:5:dst_results}
    \end{figure}

    \begin{figure}
        \centering
        \begin{subfigure}[b]{0.31\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/400_dst_ch5_5_num_backups.png}
            \caption{$\DST(10,0)$.}
            \label{fig:5:dst_backups:sub:a}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.31\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/420_dst_calm_stoch_ch5_5_num_backups.png}
            \caption{$\DST(10,\frac{1}{40})$.}
            \label{fig:5:dst_backups:sub:b}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.31\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/410_dst_stoch_ch5_5_num_backups.png}
            \caption{$\DST(10,\frac{1}{5})$.}
            \label{fig:5:dst_backups:sub:c}
        \end{subfigure}
        \caption[Number of backups performed on Deep Sea Treasure 
        environments.]{Number of backups performed on the Deep 
        Sea Treasure environments for $\DST(10,0)$, 
        $\DST(10,\frac{1}{40})$ and $\DST(10,\frac{1}{5})$ 
        respectively.}
        \label{fig:5:dst_backups}
    \end{figure}

    \paragraph{Deep Sea Treasure Environments.}

    CHVI with Prioritised Sweeping (CHVI(PS)) 
    is used as an additional baseline to CHVI, where Prioritised Sweeping \cite{sutton} performs 
    Bellman backups from timestep $t=H$ down to $t=0$.

    Results on the Deep Sea Treasure environments are given 
    in Figures \ref{fig:5:dst_results} and 
    \ref{fig:5:dst_backups}. On the deterministic 
    environment $\DST(10,0)$, CH-BTS and CH-UCT performed 
    best among the CHMCTS algorithms. On the stochastic 
    environments, CH-BTS and P-CHMCTS performed better 
    than other CHMCTS algorithms.

    However, CHMCTS algorithms generally struggled in the 
    stochastic environments. As shown in Figure 
    \ref{fig:5:dst_backups}, CHMCTS is only able to 
    perform approximately one third as many backups in the 
    stochastic environments compared to the deterministic 
    one, due to the growth of intermediate convex hull 
    value sets and the additional complexity of taking expectations over value sets (particularly the addition operation in Equation \ref{eq:2:vector_set_arithmetic_two}). With low stochasticity 
    ($\DST(10,\frac{1}{40})$), despite the performance penalty, CH-BTS is able to outperform the remaining MOMCTS algorithms, although it is still far from the optimal value that CHVI computes. With higher stochasticity 
    ($\DST(10,\frac{1}{5})$) CHMCTS is unable to run a 
    sufficient number of trials within the time budget to 
    perform well. CZT, which limits the rate of ball 
    additions, does not suffer from this issue and 
    maintains a competitive number of trials across all 
    three environments.







\subsection{Scalability Results}
\label{sec:5-5-4-scalability}

    \begin{figure}
        \centering
        % Row 1
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/700_dst_scaling_ch5_0_eum.png}
            \caption{$\DST(W,0)$ (EUM).}
            \label{fig:5:dst_scaling:sub:a}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/700_dst_scaling_ch5_2_hypervolume.png}
            \caption{$\DST(W,0)$ (NHV).}
            \label{fig:5:dst_scaling:sub:b}
        \end{subfigure}

        \vspace{1em}

        % Row 2
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/720_dst_scaling_stoch_calm_ch5_0_eum.png}
            \caption{$\DST(W,\frac{1}{40})$ (EUM).}
            \label{fig:5:dst_scaling:sub:c}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/720_dst_scaling_stoch_calm_ch5_2_hypervolume.png}
            \caption{$\DST(W,\frac{1}{40})$ (NHV).}
            \label{fig:5:dst_scaling:sub:d}
        \end{subfigure}

        \vspace{1em}

        % Row 3
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/710_dst_scaling_stoch_ch5_0_eum.png}
            \caption{$\DST(W,\frac{1}{5})$ (EUM).}
            \label{fig:5:dst_scaling:sub:e}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.45\textwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/ch5/710_dst_scaling_stoch_ch5_2_hypervolume.png}
            \caption{$\DST(W,\frac{1}{5})$ (NHV).}
            \label{fig:5:dst_scaling:sub:f}
        \end{subfigure}

        \caption[Scalability results on Deep Sea Treasure 
        environments.]{Scalability results on the Deep Sea 
        Treasure environments. (Left) Expected Utility Metric 
        (EUM). (Right) Normalised Hypervolume (NHV). Each row 
        shows results for $\DST(W,0)$, $\DST(W,\frac{1}{40})$ 
        and $\DST(W,\frac{1}{5})$ respectively, for widths $W$ 
        ranging from 10 to 100. Each algorithm was allowed up to 
        16\,GB of memory; the curves for CHVI and CHVI(PS) are 
        truncated where memory was exhausted.}
        \label{fig:5:dst_scaling}
    \end{figure}

    The \textit{normalised hypervolume} (NHV) is used in 
    this experiment, where objective values are normalised 
    to $[0,1]$ before computing the hypervolume, so that 
    results are comparable across environments of different 
    scales. As can be seen from the CHVI curves, the 
    optimal normalised hypervolume is consistently around 
    $0.8$ across all map sizes.

    Results are given in Figure \ref{fig:5:dst_scaling}. 
    On the deterministic environment $\DST(W,0)$, CHVI is 
    able to solve environments up to a width of 
    approximately $W=50$, beyond which the state space has 
    grown too large for CHVI to converge within the time 
    budget. At $W=80$, CHVI exhausts the 16\,GB memory 
    limit. CH-BTS maintains strong performance across all 
    map sizes, with a normalised hypervolume consistently 
    above $0.4$. Its EUM and NHV start close to optimal 
    for small maps, are outperformed by CHVI for 
    intermediate sizes, but degrade at a much slower rate 
    than CHVI as the map size increases, remaining 
    competitive even at $W=100$.

    On both stochastic environments, all convex hull-based 
    algorithms struggle. The performance of CHVI(PS) drops 
    sharply at around $W=25$, while CHVI degrades more 
    gradually. This is consistent with the results of 
    Section \ref{sec:5-5-3-results_and_discussion}, where 
    stochasticity significantly increases the 
    computational cost of convex hull backups.

    CZT performs best on the larger stochastic 
    environments, maintaining its performance 
    where convex hull-based algorithms struggle from the computational complexity discussed in Section \ref{sec:5-5-3-results_and_discussion}.
    However, across all environments CZT tends to converge to a locally optimal solution, collecting nearby treasure quickly without sufficiently exploring the environment to discover higher-value options further from the starting state.

















% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\section{Theoretical Discussion}
\label{sec:5-6-theory}

    This section \bd{provides some theoretical discussion around CHMCTS and MOMCTS in general.} It is structured as follows:
    \begin{itemize}
        \item Section \ref{sec:5-6-1-context_independent_policies} shows that context independent policies suffer linear contextual regret;
        \item Section \ref{sec:5-6-2-fundamental_assumption_of_momcts} discusses a fundimental assumption made in MOMCTS algorithms and \bd{shows that MOMCTS algorithms using this assumption can only compute optimal policies for linear utility functions};
        \item Section \ref{sec:5-6-3-convergence_of_chmcts} outlines \bd{but doesnt actually bother to prove} convergence proofs for CHMCTS.
    \end{itemize}

    


\subsection{Context Independent Policies}
\label{sec:5-6-1-context_independent_policies}

    1. PREAMBLE
    2. Figure
    3. THeorem copy



\subsection{The Fundamental Assumption of MOMCTS}
\label{sec:5-6-2-fundamental_assumption_of_momcts}

    \todo{move things from the theorem to preamble. Like move the greedy policy and call it pi local. Call the theorem "locally optimal policy requires linear utility"}

    1. Preamble - define the setup
    2. General utility, and the form of the search/recommendation policy
    3. Wordy description of the offending MOMDP with chebyshev utility
    4. Theorem

    % The essential feature of your example is that b=cb = c
    % b=c dominates aa
    % a in the second coordinate and dd
    % d in the first coordinate — this is what makes bb
    % b (and cc
    % c) "block" *both* aa
    % a and dd
    % d simultaneously under any weighting.



    \begin{theorem}[Per-step action selection requires linear utility]
        \label{thm:per-step-linear}
        Let $\bfcl{M}$ be an MOMDP, $\Pi$ the set of all policies and the optimal value sets be:
        %
        \begin{align*}
            \bfcl{V}^*(s) &= \{\bff{V}^{\pi}(s) \mid \pi \in U(\Pi;u)\}, \\
            \bfcl{Q}^*(s, a) &= \{\bff{Q}^{\pi}(s, a) \mid \pi \in U(\Pi;u)\},
        \end{align*}
        %
        where $U(\Pi;u)$ is the undominated set of policies (Definition \ref{def:2:undominated_set}) and $u$ is a utility function. Let $\pi^{\bff{w}}_u \in \argmax_{\pi \in \Pi} u(\bff{V}^{\pi}(s_0); \bff{w})$ denote a utility-optimal policy. Consider the greedy selection rule
        %
        \begin{equation*}
            \pi_{\mathrm{greedy}}(s; \bff{w}) \in \argmax_{a \in \cl{A}}\;
                \max_{\bff{q} \in \bfcl{Q}^*(s, a)} u(\bff{q}; \bff{w}).
        \end{equation*}
        %
        Then $\pi_{\mathrm{greedy}}(\cdot; \bff{w})$ is utility-optimal (i.e. $u(\bff{V}^{\pi_{\mathrm{greedy}}}(s_0); \bff{w}) = u(\bff{V}^{\pi^{\bff{w}}_u}(s_0); \bff{w})$) for every MOMDP $\bfcl{M}$ and every $\bff{w} \in \Delta^D$ if and only if $u(\cdot; \bff{w})$ is linear in its first argument for every
        $\bff{w} \in \Delta^D$.
    \end{theorem}
    \begin{proofoutline}
        \bd{If direction.}
        Suppose $u(\cdot; \bff{w})$ is linear in its first argument for every 
        $\bff{w} \in \Delta^D$, i.e. $u(\bff{v}; \bff{w}) = \bff{w} \cdot \bff{v}$ 
        (up to a $\bff{w}$-dependent constant, which does not affect the $\argmax$). 
        Then $U(\Pi; u)$ coincides with the convex coverage set, and the greedy 
        selection rule reduces to standard single-objective action selection under 
        the scalarised reward $\bff{w} \cdot \bff{r}(s, a)$. Utility-optimality of 
        $\pi_{\mathrm{greedy}}$ then follows from the correctness of convex hull 
        value iteration (Theorem~\todo{ref CHVI}), which reduces to standard value 
        iteration for each fixed $\bff{w}$.
    
        \bd{Only if direction.}
        We prove the contrapositive by exhibiting a single MOMDP and utility 
        function for which $\pi_{\mathrm{greedy}}$ is not utility-optimal. Let $u$ 
        be the Chebyshev utility with reference point $\bff{z}^* = (6, 6)$,
        %
        \begin{equation*}
            u(\bff{v}; \bff{w}) = -\max_i w_i \left| v_i - z^*_i \right|,
        \end{equation*}
        %
        which is non-linear in its first argument. Consider the MOMDP in 
        Figure~\todo{ref fig}, which has a single action $a_0$ at $s_0$ 
        transitioning with equal probability to successor states $s_1$ or $s_2$, 
        at each of which the agent selects between $a_0$ and $a_1$ to receive a 
        terminal vector reward. The four deterministic paths yield
        %
        \begin{align*}
            s_0 \to s_1 \to a_0 &: (5, 0), &
            s_0 \to s_1 \to a_1 &: (2, 2), \\
            s_0 \to s_2 \to a_0 &: (2, 2), &
            s_0 \to s_2 \to a_1 &: (0, 5).
        \end{align*}
        %
        Fix the preference $\bff{w} = (\tfrac{1}{2}, \tfrac{1}{2})$. By inspection, 
        the four stationary policies yield expected values at $s_0$ of $(3.5, 1)$, 
        $(2.5, 2.5)$, $(2, 2)$, and $(1, 3.5)$, with the policy achieving 
        $(2.5, 2.5)$ being utility-optimal:
        %
        \begin{equation*}
            u((3.5, 1); \bff{w}) = -2.5, \quad
            u((2.5, 2.5); \bff{w}) = -1.75, \quad
            u((1, 3.5); \bff{w}) = -2.5.
        \end{equation*}
        %
        (The value $(2, 2)$ is Pareto-dominated and is omitted.) The utility-optimal 
        policy selects $a_0$ at $s_1$ (receiving $(5, 0)$) and $a_1$ at $s_2$ 
        (receiving $(0, 5)$), giving expected value $(2.5, 2.5)$ and utility $-1.75$.
    
        Now consider $\pi_{\mathrm{greedy}}$. The Pareto fronts at the successor 
        states are singletons,
        %
        \begin{align*}
            \bfcl{Q}^*(s_1, a_0) &= \{(5, 0)\}, &
            \bfcl{Q}^*(s_1, a_1) &= \{(2, 2)\}, \\
            \bfcl{Q}^*(s_2, a_0) &= \{(2, 2)\}, &
            \bfcl{Q}^*(s_2, a_1) &= \{(0, 5)\},
        \end{align*}
        %
        and the Chebyshev utilities are $u((5, 0); \bff{w}) = u((0, 5); \bff{w}) = -3$ 
        and $u((2, 2); \bff{w}) = -2$. Hence $\pi_{\mathrm{greedy}}$ selects $a_1$ at 
        $s_1$ and $a_0$ at $s_2$, both yielding $(2, 2)$. The expected value at $s_0$ is
        %
        \begin{equation*}
            \bff{V}^{\pi_{\mathrm{greedy}}}(s_0) 
            = \tfrac{1}{2}(2, 2) + \tfrac{1}{2}(2, 2) = (2, 2),
        \end{equation*}
        %
        with utility $u((2, 2); \bff{w}) = -2 < -1.75$. Therefore 
        $\pi_{\mathrm{greedy}}$ is not utility-optimal.
    
        The obstruction is structural: under Chebyshev utility the optimal policy 
        requires selecting the \emph{locally} worst continuation ($(5, 0)$ at $s_1$ 
        and $(0, 5)$ at $s_2$), whose expectation across the stochastic transition 
        yields a \emph{globally} preferable balanced outcome. No per-state $\argmax$ 
        on $u$ can discover this coordination, because $u$ does not commute with the 
        expectation over successor states. Linearity of $u$ is precisely the 
        condition under which this commutation holds, and hence precisely the 
        condition under which per-state action selection is utility-optimal.
    \end{proofoutline}







    Theory results:
    \begin{itemize}
        \item Non-contextual policies garunteed to have a linear contextual simple regret
        \item Convex Hull algorithms garunteed to converge (lemma that UCT style policy picks actions inf often and reuse ch4 result for BTS), and mainly reproduce the result from CHVI
        \item Highlight that one step of the convex hull proof holds iff linear utility, use it to say that MOMCTS with value sets doesn't really work for non-linear utility functions
        \item Reconcile with prior work by saying that the histories are taken into account, and the pareto fronts stored are globally
        \item But generally highlight that this is a limitation of using MOMCTS, that optimisation at the level of actions is correct if and only if the utility function is linear. Give an example MOMDP where to get the optimal value doesnt allow for chebychev scalarization over value sets
        \item Discuss the difference between Contextual Zooming and CZT, cite a doubling trick and say that it can be used to generalise CZ to an algorithm for CMAB problems without a fixed number of rounds. Hypothesise that CZT contextual cumulative regret matches that of CZ, but the proofs of CZ are very complex themselves, and UCT theory is also long and hard, so not going to attempt to prove it.
    \end{itemize}

    
    From Van Moffaert paper: %https://jmlr.org/papers/volume15/vanmoffaert14a/vanmoffaert14a.pdf

    As a result, the Bellman equation no longer holds and the learning algorithm is not proven to converge to the optimal policy (Perny and Weng, 2010; Roijers et al., 2013). Nevertheless, even without this theoretical guarantee, a Chebyshev scalarized MORL algorithm can obtain high-quality solutions (Van Moffaert et al., 2013b). 


   




New Ch5 plan:
\begin{itemize} 
    \item Theory, contextual regret, non-contextual policies garunteed to have a linear contextual regret
    \item Theory, reproduce the CHMCTS results, and say that garunteed to eventually produce optimal (lemmas are actions selected inf often)
    \item No theory for CZT directly, makes a heuristic adaptation from the base work, and the proofs are very complex in themselves (would add like 50 pages of maths) 
\end{itemize}



for theory would just like to have convergence, use ch4 proofs and UCT picks actions inf often as lemma and use CHVI proofs 

Allude to that boltzmann probably gives exp contextual simple regret, and PUCT gives similar contextual cumulative regret, and the lower bound from exploring bandits means simple regret of UCT methods has a lower bound

\todo{Update theory appendix to be more general theory appendix. First section is additional results from literature, like hoeffdings. Include proof of UCT policy selecting any action infinitely often for an arbitrary Q value here. second section is exponential consistency proofs, split into three subsections, dents process, preliminary lemmas and the proofs.}





also more critical to look into litrev for what results about MORL there are, shimons paper probably helpful

want to cite if someone has done that linear scalarisation iff making local decisions result


%
% Original brain dumping on this
%

\todo{part 1: all the (relevant) search policies will select actions infinitely often.}

\todo{part 1a: BTS and DENTS follows from ch4. For UCT can argue that if select one argument a finite number of times. Then use the confidence interval term to arrive at contradiction}

\todo{part 2: convex hull backups perform the scalarised backups for all possible weights over the objective.}

\todo{write up proofs wrote up on ipad. Used this gpt convo in cleaning up details: https://chatgpt.com/c/67fa9295-0cb8-8007-88fd-5bec1ac5393b}

\todo{part 2a: refer back to convergence of backups in ch4, clean up and DKW stuff need to}

\todo{part 3: if values converge to optimal, then contextual simple regret tends to zero}

\todo{part 4: mention that CZT proofs would be much more complex, and given the other algorithms do better. Mention a lower bound which comes from the CZ regret bound. And that this algorithm could be preferable in not in exploring context.}

\todo{part 4a: similar with CH-UCT to CZT}

\todo{part 5: any proofs for CH-UCT and/or CZT? Which wouldn't be a disaster trying to proove?



}




Below is what we initially wrote in this section.

\todo{See if with chatgpt help can crack out a rough proof outline? Because assuming no noise in the reward function, the leaf nodes clearly converge with same rate. Just really need the induction step. Feel like it should be very do-able, by bootstrapping on the CZT proofs.}

\todo{Can easily show that CH converges, by using every action sampled infinitly often still, and bootstrap off the convex hull convergence proof.}

\todo{One thing need to prove is that our form of backups converges, because we're using the sample averages our backups are slightly different. Should}

\todo{Remainder of properties bootstrap off the proofs in Ch4. Every action is sampled inf often, hence the convex hull backups converge. Don't bother showing convergence rates. Just show that they are consistent.}

\todo{Also want to show that given any convex hull value estimates, that following the policy extracted from convex hulls does give the same multi-objective value.}

basically this would say
\begin{align}
    \pi(s;w) &= \argmax_{a\in\cl{A}} \circ{\bff{Q}}(s,a;w) \\
    w^\top\bff{V}^\pi(s;w) &= w^\top \bb{E}_{a\sim\pi}[ \bff{Q}^\pi(s,a;w)] \\
        &= \circ{\bff{Q}}(s,a;w) \\
    w^\top\bff{Q}^\pi(s,a;w) &= ...    
\end{align}

So say that the value of the policy by taking the maimum from value estimates, will achieve the value estimates. We've been handwavy around empirical distriubtion vs actual transition distribution. But basically the argument is inductive, and holds trivially for leaf nodes.






% Chebyshev scalarisation proof
% Assumes standard amsmath / amssymb / amsthm setup.
 
\begin{prop}
    Let $u(w, x, z) = \min_{i} w_i \lvert x_i - z_i \rvert$ denote the Chebyshev
    scalarisation, and consider the vectors
    \[
      a = (10, 0), \quad b = c = (1, 1), \quad d = (0, 10),
    \]
    together with a reference point $z \in \mathbb{R}^2$ satisfying $z_i < \min\{a_i, b_i, c_i, d_i\}$
    componentwise. Let $w = (w_1, w_2)$ be any weight vector with $w_1, w_2 > 0$ and
    $w_1 + w_2 = 1$. Then
    \[
      u(w, b, z) > u(w, a, z) \quad \text{or} \quad u(w, c, z) > u(w, d, z).
    \]
\end{prop}
     
    \begin{proof}
    The componentwise inequality $z_i < \min\{a_i, b_i, c_i, d_i\}$ permits the absolute
    values to be dropped, so that
    \[
      u(w, x, z) = \min_{i} w_i (x_i - z_i)
    \]
    for each of $x \in \{a, b, c, d\}$. The shifted coordinates
    \[
      \alpha_i = a_i - z_i, \qquad
      \beta_i  = b_i - z_i = c_i - z_i, \qquad
      \delta_i = d_i - z_i
    \]
    are therefore all strictly positive. From $z_1 < 0$ and $z_2 < 0$ (which follow from
    $z < d$ and $z < a$ respectively), the following strict inequalities are obtained:
    \begin{equation}\label{eq:key-ineqs}
      \alpha_2 = -z_2 < 1 - z_2 = \beta_2,
      \qquad
      \delta_1 = -z_1 < 1 - z_1 = \beta_1.
    \end{equation}
    Since $b = c$, the equality $u(w, b, z) = u(w, c, z)$ holds throughout.
     
    Suppose, for contradiction, that both disjuncts fail, i.e.
    \begin{align}
      \min(w_1 \alpha_1,\, w_2 \alpha_2) &\geq \min(w_1 \beta_1,\, w_2 \beta_2), \label{eq:A} \\
      \min(w_1 \delta_1,\, w_2 \delta_2) &\geq \min(w_1 \beta_1,\, w_2 \beta_2). \label{eq:D}
    \end{align}
     
    From \eqref{eq:A}, the left-hand side is bounded above by $w_2 \alpha_2$, so
    \[
      w_2 \alpha_2 \;\geq\; \min(w_1 \beta_1,\, w_2 \beta_2).
    \]
    By \eqref{eq:key-ineqs} we have $w_2 \alpha_2 < w_2 \beta_2$, whence the minimum on
    the right cannot equal $w_2 \beta_2$. It follows that
    \begin{equation}\label{eq:from-A}
      \min(w_1 \beta_1,\, w_2 \beta_2) = w_1 \beta_1, \qquad \text{so} \qquad w_1 \beta_1 \leq w_2 \beta_2.
    \end{equation}
     
    By the analogous argument applied to \eqref{eq:D}, using $w_1 \delta_1 < w_1 \beta_1$,
    \begin{equation}\label{eq:from-D}
      \min(w_1 \beta_1,\, w_2 \beta_2) = w_2 \beta_2, \qquad \text{so} \qquad w_2 \beta_2 \leq w_1 \beta_1.
    \end{equation}
     
    Combining \eqref{eq:from-A} and \eqref{eq:from-D} yields $w_1 \beta_1 = w_2 \beta_2$.
    Substituting back into \eqref{eq:A} gives $w_2 \alpha_2 \geq w_1 \beta_1 = w_2 \beta_2$,
    i.e.\ $\alpha_2 \geq \beta_2$, contradicting \eqref{eq:key-ineqs}.
     
    The assumption that both disjuncts fail is therefore untenable, and at least one of
    $u(w, b, z) > u(w, a, z)$ or $u(w, c, z) > u(w, d, z)$ must hold.
    \end{proof}
     
    \begin{remark}
    The argument relies only on the strict inequalities $a_2 < b_2$ and $d_1 < c_1$
    (together with $b = c$). Consequently the proposition generalises: whenever a common
    point $b = c$ strictly dominates $a$ in one coordinate and $d$ in the other, no
    single Chebyshev weight vector can simultaneously render both $a$ preferable to $b$
    and $d$ preferable to $c$.
    \end{remark}









\subsection{Convergence of CHMCTS}
\label{sec:5-6-3-convergence_of_chmcts}

    1. Preamble - say not going to actually prove things
    2. Quote CHVI theorem
    3. Define optimal CHVS (or check that it was defined in Ch2)
    4. CHMCTS values converge to optimal CHVS (proof = it would contradict CHVI theorem)
    6. General UCB picks all actions inf often
    5. Just give an example translation from DENTS results to CHMCTS results
    7. CHUCT converges
























% [ ] First Draft
% [ ] AI proofread/grammar check
% [ ] TODOs
% [ ] Spell check
% [ ] Citations + Refs
% [ ] LitRev sync
% [ ] Abbreviations sync
% [ ] Thesis questions ref/answers
% [ ] Proofread
% [ ] Nick comments
% [ ] Final revision
\section{Summary}
\label{sec:5-7-summary}

    \begin{itemize}
        \item This chapter introduced Contextual Zooming for Trees (CZT), a contextual multi-armed bandit algorithm that is used for decision making in multi-objective settings.
        \item The chapter also introduced Convex Hull Monte Carlo Tree Search (CHMCTS), which generalises the prior work in multi-objective MCTS to handle stochastic environments by using backups over Convex Hull Value Sets.
        \item The chapter also discussed the theoretical properties of the CZT and CHMCTS algorithms.
    \end{itemize}

    New Ch5 plan:
    \begin{itemize} 
        \item Conclusion, wrap up, motivate move to simplex maps (convex hull slow) and weight space (entropy)
    \end{itemize}

    future work includes using the biasing terms from Ch4 in these algorithms too, currently result essentially set the psuedo visits to 0. 

    future work includes using more efficient weight space data structure (value curves) instead of convex hulls





    From Van Moffaert paper: %https://jmlr.org/papers/volume15/vanmoffaert14a/vanmoffaert14a.pdf
    Lizotte et al. (2010) reduce the asymptotic space and time complexity of the bootstrapping rule by simultaneously learning several value functions corresponding to different weights and by calculating their piecewise linear spline representation. Recently, they validated their work on clinical trial data for three objectives, although the practical possibilities for higher dimensional spaces are not straightforward (Lizotte et al., 2012).




